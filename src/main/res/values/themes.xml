<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MyFirstApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.MyFirstApp" parent="Base.Theme.MyFirstApp" />
    <style name="Dialogtheme" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="colorAccent">#FF5722</item> <!-- Button highlight color -->
        <item name="android:textColorPrimary">#FFFFFF</item> <!-- Text color -->
        <item name="android:background">#222222</item> <!-- Background color -->
    </style>

    <style name="TransparentActivityTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:backgroundDimEnabled">false</item>
    </style>
</resources>